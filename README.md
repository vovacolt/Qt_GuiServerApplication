# GUI Server Application
TCP-сервер с графическим интерфейсом, предназначенный для управления удаленными устройствами и мониторинга поступающих от них метрик в режиме реального времени.

<img width="1294" height="761" alt="image" src="https://github.com/user-attachments/assets/62e839c4-5879-4e88-a464-42114ef1dab0" />

Система состоит из двух приложений:
1.  **Server (GUI)**: Приложение с графическим интерфейсом для управления подключениями и отображения полученных данных в табличном виде.
2.  **[Client (Console)](https://github.com/vovacolt/Qt_ConsoleClientApplication/tree/main)**: Консольное приложение, эмулирующее работу устройства. Генерирует и отправляет случайные данные (метрики, статусы, логи) на сервер.

## 📋 Основные возможности
* **Сетевой узел**: Слушает порт `12345` и поддерживает стабильное одновременное соединение с группой клиентов.
* **Параллельная обработка**: Сетевая логика полностью отделена от GUI-потока, что исключает «зависание» интерфейса при интенсивном обмене данными.
* **JSON-протокол**: Взаимодействие построено на обмене объектами `QJsonObject`. Сервер автоматически подтверждает каждое успешное подключение.
* **Система фильтрации и логирования**: Ведется подробный лог событий, доступный непосредственно в окне приложения.
* **Удаленное управление**: Сервер предоставляет возможность глобальной остановки и запуска передачи данных для всех подключенных устройств.

## 🖥 Описание интерфейса
Интерфейс приложения построен на базе `QMainWindow` и оптимизирован для быстрого анализа состояния сети:

1.  **Панель подключений**: Таблица (`QTableWidget`), отображающая список активных устройств с их ID, IP-адресом и текущим статусом.
2.  **Монитор данных**: Вторая таблица для визуализации входящих пакетов. Она автоматически парсит JSON-содержимое по ключам и выводит:
    * **NetworkMetrics**: Пропускная способность, задержка, потери пакетов.
    * **DeviceStatus**: Uptime, нагрузка на CPU и использование памяти.
    * **Log**: Текстовые сообщения от устройств.
3.  **Консоль событий**: Поле `QTextEdit` для системных уведомлений.
4.  **Настройки**: Блок конфигурации критических порогов.

## 🛠 Технический стек
* **Язык**: C++
* **Библиотеки**: Qt 6.5.2 (Network, Widgets, Core)
* **Стандарт**: C++17

## 📦 Сборка и запуск

### Требования
* Установленный Qt 6.5.2 (или совместимая версия)
* Компилятор C++ (MSVC, GCC, Clang)
* QMake

### Инструкция
1.  Клонируйте репозиторий.
2.  Откройте проект в **Qt Creator** (через `.pro` файл).
3.  **Сборка Сервера**: Выберите цель `Server`, соберите и запустите.
4.  **Сборка Клиента**: Выберите цель `Client`, соберите.
5.  Запустите один или несколько экземпляров Клиента (можно через терминал или настройки запуска в Qt Creator).

## 🖥 Сценарий использования

Типовой сценарий работы системы включает следующие этапы:

1.  **Запуск сервера**: При старте приложения отображается графический интерфейс с пустыми таблицами.
2.  **Подключение клиентов**: Консольные клиенты инициируют соединение с сервером по адресу `localhost:12345`.
3.  **Авторизация**: Сервер отправляет пакеты с подтверждением подключения, которые клиент выводит в консоль.
4.  **Мониторинг подключений**: В таблице сервера появляются записи с ID клиента, его IP-адресом и статусом.
5.  **Передача данных**: Клиенты начинают отправку JSON-пакетов (метрики, статусы, логи) с варьирующейся длиной содержимого и случайной задержкой от 0.01 до 0.1 секунды.
6.  **Визуализация**: Сервер распарсит данные и отобразит их в таблице данных, фиксируя время получения.
7.  **Логирование событий**: Все события (подключение, получение данных, отключение, ошибки) выводятся в текстовый лог сервера.

## 📂 Структура проекта

* `/server` - Исходный код GUI приложения.
* `/common` - Общие файлы (если имеются).

## 📝 TODO: План развития проекта

* **Система сборки:** Добавить поддержку **CMake** (`CMakeLists.txt`).
* **Интерфейс настроек:** Разработать диалоговое окно (Settings Dialog) для редактирования параметров сервера и порогов прямо из GUI, с последующим сохранением в `config.ini`.
* **Уведомления:** Добавить визуальную индикацию (например, подсветку строки красным цветом) в `m_dataTable` при превышении полученных значений над пороговыми.
* **Логирование:** Реализовать запись всех входящих пакетов в текстовый файл на диске для последующего анализа.
* **Бесконечный виртуальный скролл:** Для реализации «бесконечного» виртуального скролла в Qt необходимо отказаться от `QTableWidget` в пользу паттерна Model/View (`QTableView` + `QAbstractTableModel`).
